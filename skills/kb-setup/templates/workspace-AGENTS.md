# Workspace Agent Instructions

> {{USER_NAME}}'s workspace root. Last setup: **{{DATE}}**.

This file is the **master index** for the workspace. Read it first, then jump to the repo relevant to your task.

## Repos in this workspace

{{REPO_INDEX}}

## Short aliases

Use these abbreviations anywhere — the agent resolves them to the full repo path.

{{ALIAS_INDEX}}

## Rules

- Each repo has its own `AGENTS.md`. Read it before working in that repo.
- Cross-repo changes require coordination — check rules in the relevant repo's `AGENTS.md`.
- `/kb` is the single command for all knowledge operations (see `my-kb/AGENTS.md`).

## Keyword lookup

When the user mentions a concept, use this table to jump directly to the right file.

{{KEYWORD_LOOKUP}}

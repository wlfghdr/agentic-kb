# Reference: artifact output contract

Every shipped `kb_roadmap.py` run emits three files with the same base name under the scope-specific directory:

```
<output-dir>/<scope>/roadmap-<YYYY-MM-DD>.md
<output-dir>/<scope>/roadmap-<YYYY-MM-DD>.html
<output-dir>/<scope>/roadmap-<YYYY-MM-DD>.json
```

For roll-up scopes, the base name includes the scope explicitly: `roadmap-<scope>-<YYYY-MM-DD>.{md,html,json}`. Each successful write also refreshes `<output-dir>/index.html` so the latest artifact per scope is linkable from one stable root page.

## JSON sidecar (authoritative)

The JSON is the source of content for the current helper-script outputs; MD and HTML are renderings. The richer schema in `templates/roadmap.json.schema.json` remains the draft target, but the shipped helper currently emits this smaller stable payload:

Top-level shape:

```json
{
  "generated_at": "2026-05-08T14:30:00",
  "scope": "product-a",
  "date": "2026-05-08",
  "generator": "kb-roadmap",
  "items": [
    {
      "id": "ABC-101",
      "title": "Planned item",
      "status": "In Progress",
      "phase": "in-delivery",
      "tracker": "plan-export"
    }
  ],
  "correlations": [
    { "id": "ABC-101", "appearances": 2, "trackers": ["delivery-export", "plan-export"] }
  ],
  "summary": {
    "total": 2,
    "by_phase": {
      "idea": 0,
      "defined": 0,
      "committed": 0,
      "in-delivery": 1,
      "shipped": 1,
      "archived": 0
    },
    "correlated": 1,
    "single_tracker": 0
  },
  "warnings": []
}
```

## Markdown report

Human-readable. The shipped helper always emits the seven section headings in order, even when a section is still a pilot placeholder.

```markdown
# Roadmap — <scope> — <date>

> <timeframe description>
> <counts summary line>

## A. Plan baseline
...

## B. Delivery baseline
...

## C. Correlation matrix
<table: tier | count | examples>

## D. Delta since <since-date>
...

## E. Mismatch findings
### Delivered, unplanned
### Planned, undelivered
### Traceability gaps
### Stalled

## F. Forward plan
...

## G. Decisions needed
...

---
Generated by kb-roadmap v<version> · <timestamp>
```

## HTML report

The shipped helper renders an inline self-contained HTML artifact with the following guaranteed sections:

- Sticky top nav
- Theme toggle (light / dark / auto), persisted in localStorage
- Optional logo slot in the hero/header
- Timeline section and status-board section are always present, even when thin
- WCAG AA contrast in both themes
- Print-friendly fallback (`@media print` hides nav and theme toggle, collapses to single column)
- No external network dependencies — fonts, icons, styles all inline
- No JavaScript required for content visibility (JS is only used for theme toggle and timeline controls)

## Styling tokens

The template reads CSS custom properties from the adopter's `.kb-config/artifacts.yaml` `html-template.tokens` setting:

- `neutral` (default) — muted greys, high-contrast accent
- A file path — a CSS file that overrides the token block (adopters supply brand palettes here)

The skill itself ships no brand colors or logos. Adopters that want a branded artifact provide:

```yaml
html-template:
  base: kb-roadmap/templates/roadmap.html
  tokens: .kb-references/brand/tokens.css
  logo:
    light: .kb-references/brand/logo-light.svg
    dark: .kb-references/brand/logo-dark.svg
```

## Root index

`<output-dir>/index.html` is refreshed after every non-dry-run render. It lists the latest HTML artifact per scope and links directly into the scope folder.

## Versioning

The helper's JSON payload does not currently carry the SKILL version field. Compatibility is tracked through the scope/date filename contract plus the root `CHANGELOG.md`. The richer versioned schema remains the draft target.

## Changelog

| Date | What changed | Source |
|------|-------------|--------|
| 2026-05-08 | Aligned the artifact contract with the shipped helper output shape, roll-up filename rule, and root index refresh behavior | Integration pass |
